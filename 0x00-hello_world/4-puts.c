#include <stdio.h>

/**
 * main - Use the function puts to print the strings
 * Description: Print some strings with puts
 * Return: 0(success)
 */
int main(void)
{
	puts("Programming is like building a multilingual puzzle");
	return (0);
}
